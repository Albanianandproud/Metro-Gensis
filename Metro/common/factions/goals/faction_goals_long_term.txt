
faction_goal_resource_control = {
	name = resource_control_goal_name
	description = resource_control_goal_desc
	category = long_term
	group = resources_to_market_icon
	visible = {

	}
	available = {
		
	}
	completed = {
		has_resources_in_collection = {
			collection = {
					input = game:scope
					operators =  { faction_members }
					name = FACTION
			}
			resource = oil
			amount > 199
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = {
					input = game:scope
					operators =  { faction_members }
					name = FACTION
			}
			resource = chemicals
			amount > 199
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = {
					input = game:scope
					operators =  { faction_members }
					name = FACTION
			}
			resource = electronics
			amount > 199
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = {
					input = game:scope
					operators =  { faction_members }
					name = FACTION
			}
			resource = scrap
			amount > 199
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = {
					input = game:scope
					operators =  { faction_members }
					name = FACTION
			}
			resource = components
			amount > 199
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = {
					input = game:scope
					operators =  { faction_members }
					name = FACTION
			}
			resource = alloys
			amount > 199
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
	}
	complete_effect = {
		add_faction_power_projection = 500
		custom_effect_tooltip = generic_skip_one_line_tt
		custom_effect_tooltip = THE_FACTION_LEADER
		add_faction_initiative = 1
		custom_effect_tooltip = generic_skip_one_line_tt
		every_faction_member = { 
			tooltip = INFLUENCE_TOP_THREE
			visible_when_empty = yes
			limit = {
				faction_influence_rank < 4
			}
			add_political_power = 100
		}
	}
	ai_will_do = {
		base = 1
	}
}






#faction_goal_long_resource_consolidation = {}
#
#faction_goal_long_sokolnicheskaya = {}
#faction_goal_long_zamoskvoretskaya = {}
#faction_goal_long_arbatsko_pokrovskaya = {}
#faction_goal_long_filovskaya = {}
#faction_goal_long_koltsevaya = {}
#faction_goal_long_kaluzhsko_rizhskaya = {}
#faction_goal_long_tagansko_krasnopresnenskaya = {}
#faction_goal_long_kalininsko_solntsevskaya = {}
#faction_goal_long_serpukhovsko_timiryazevskaya = {}
#faction_goal_long_lyublinsko_dmitrovskaya = {}
#faction_goal_long_kakhovskaya = {}
#
#faction_goal_long_community_victory = {}
#faction_goal_long_council_victory = {}
#faction_goal_long_oligarch_victory = {}
#faction_goal_long_dictatorial_victory = {}
#faction_goal_long_bandit_victory = {}
#
#faction_goal_long_communist_triumph = {}
#faction_goal_long_fascist_triumph = {}
#faction_goal_long_order_triumph = {}
#faction_goal_long_capitalist_triumph = {}
#faction_goal_long_bandit_victory = {}
#
#faction_goal_long_recolonize_metro = {}
#faction_goal_long_become_dominant_powerhouse = {}