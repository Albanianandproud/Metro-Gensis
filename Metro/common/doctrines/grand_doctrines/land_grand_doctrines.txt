elite_doctrine = {
    folder = land

    name = GRAND_DOCTRINE_ELITE
    description = GRAND_DOCTRINE_ELITE_DESC
    icon = GFX_doctrine_mobile_warfare_medium

    available = {
        always = yes
    }

    xp_cost = 75
    xp_type = army

    tracks = {
        doctrine_track_soldiers
        doctrine_track_command
        #doctrine_track_combat_support
        doctrine_track_tactics
    }

    # EFFECTS
    enable_tactic = tactic_unexpected_thrust
	
    milestones = {
        {
            #doctrine_track_soldiers
            out_of_supply_factor = -0.1
            effect = {
                if = {
                    limit = {
                        NOT = {
                            has_variable = infantry_milestone_var
                        }
                    }
                    set_variable = { infantry_milestone_var = 1 }
                }
                else = {
                    add_to_variable = { infantry_milestone_var = 1 }
                }
            }
        }
        {
			#doctrine_track_combat_support
            max_command_power = 50
            effect = {
                if = {
                    limit = {
                        NOT = { has_variable = command_milestone_var
                        }
                    }
                    set_variable = { command_milestone_var = 1 }
                }
                else = {
                    add_to_variable = { command_milestone_var = 1 }
                }
            }
        }
        {
			#doctrine_track_tactics
            effect = {
                if = {
                    limit = {
                        NOT = {  has_variable = operations_milestone_var }
                    }
                    set_variable = { operations_milestone_var = 1 }
                }
                else = {
                    add_to_variable = { operations_milestone_var = 1 }
                }
            }
        }
    }

	ai_will_do = {
        base = 1
		modifier = {
            factor = 5
            original_tag = SPT
			original_tag = PLS
			original_tag = MDR
        }
        modifier = {
            factor = 0.05
            original_tag = RED
        }
    }
}
conventional_doctrine = {
    folder = land

    name = GRAND_DOCTRINE_CONVENTIONAL
    description = GRAND_DOCTRINE_CONVENTIONAL_DESC
    icon = GFX_doctrine_mobile_warfare_medium

    available = {
        always = yes
    }

    xp_cost = 75
    xp_type = army

    tracks = {
        doctrine_track_soldiers
        doctrine_track_command
        #doctrine_track_combat_support
        doctrine_track_tactics
    }

    # EFFECTS
    enable_tactic = tactic_planned_attack
	
    milestones = {
        {
            #doctrine_track_soldiers
            max_dig_in_factor = 0.1
            effect = {
                if = {
                    limit = {
                        NOT = {
                            has_variable = infantry_milestone_var
                        }
                    }
                    set_variable = { infantry_milestone_var = 1 }
                }
                else = {
                    add_to_variable = { infantry_milestone_var = 1 }
                }
            }
        }
        {
			#doctrine_track_combat_support
            max_command_power = 50
            effect = {
                if = {
                    limit = {
                        NOT = { has_variable = command_milestone_var
                        }
                    }
                    set_variable = { command_milestone_var = 1 }
                }
                else = {
                    add_to_variable = { command_milestone_var = 1 }
                }
            }
        }
        {
			#doctrine_track_tactics
            effect = {
                if = {
                    limit = {
                        NOT = {  has_variable = operations_milestone_var }
                    }
                    set_variable = { operations_milestone_var = 1 }
                }
                else = {
                    add_to_variable = { operations_milestone_var = 1 }
                }
            }
        }
    }

	ai_will_do = {
        base = 1
		modifier = {
            factor = 5
            original_tag = ARB
			original_tag = HNZ
			original_tag = RCH
        }
        modifier = {
            factor = 0.05
            original_tag = RED
        }
    }
}
overwhelm_doctrine = {
    folder = land

    name = GRAND_DOCTRINE_OVERWHELM
    description = GRAND_DOCTRINE_OVERWHELM_DESC
    icon = GFX_doctrine_mobile_warfare_medium

    available = {
        always = yes
    }

    xp_cost = 75
    xp_type = army

    tracks = {
        doctrine_track_soldiers
        doctrine_track_command
        #doctrine_track_combat_support
        doctrine_track_tactics
    }

    # EFFECTS
	enable_tactic = tactic_breakthrough

    milestones = {
        {
            #doctrine_track_soldiers
            pocket_penalty = -0.2
            effect = {
                if = {
                    limit = {
                        NOT = {
                            has_variable = infantry_milestone_var
                        }
                    }
                    set_variable = { infantry_milestone_var = 1 }
                }
                else = {
                    add_to_variable = { infantry_milestone_var = 1 }
                }
            }
        }
        {
			#doctrine_track_combat_support
            max_command_power = 50
            effect = {
                if = {
                    limit = {
                        NOT = { has_variable = command_milestone_var
                        }
                    }
                    set_variable = { command_milestone_var = 1 }
                }
                else = {
                    add_to_variable = { command_milestone_var = 1 }
                }
            }
        }
        {
			#doctrine_track_tactics
            effect = {
                if = {
                    limit = {
                        NOT = {  has_variable = operations_milestone_var }
                    }
                    set_variable = { operations_milestone_var = 1 }
                }
                else = {
                    add_to_variable = { operations_milestone_var = 1 }
                }
            }
        }
    }

	ai_will_do = {
        base = 1
		modifier = {
            factor = 5
            original_tag = RED
        }
        modifier = {
            factor = 0.05
            original_tag = SPT
			original_tag = HNZ
			original_tag = PLS
			original_tag = MDR
        }
    }
}