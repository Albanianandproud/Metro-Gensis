### Reclaimation Decisions ###
    reclaim_metro = {
    	icon = generic_prospect_for_resources
        visible = {
            NOT = { #To ensure Majors don't swallow up everything
                TAG = HNZ
                TAG = RED
                TAG = PLS
                TAG = RCH
                TAG = VDN #Because Reasons
		TAG = GWC #They got there own thing goin on
            }
            OR = {
                is_neighbor_of = RUI
            }
        }
        priority = {
		    base = 0
	    }
    }
