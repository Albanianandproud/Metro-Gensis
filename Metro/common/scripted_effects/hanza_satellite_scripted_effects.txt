hanza_satellite_improve_relations_scripted_effect = {
	FROM = {
		add_opinion_modifier = {
			target = ROOT
			modifier = hanza_satellite_trade
		}
	}
	set_temp_variable = { bullet_to_gain = 50 }
	add_bullet_with_tooltip_effect = yes
}
hanza_satellite_trade_bastion_scripted_effect = {
	ROOT = {
		add_opinion_modifier = {
			target = FROM
			modifier = hanza_satellite_trade
		}
	}

	add_timed_idea = {
		idea = hanza_satellite_trade_bastion_modifier
		days = 45
	}

	set_temp_variable = { bullet_to_gain = -50 }
	add_bullet_with_tooltip_effect = yes
}
hanza_satellite_trade_commerce_scripted_effect = {
	ROOT = {
		add_opinion_modifier = {
			target = FROM
			modifier = hanza_satellite_trade
		}
	}

	add_timed_idea = {
		idea = hanza_satellite_trade_commerce_modifier
		days = 45
	}

	set_temp_variable = { bullet_to_gain = -50 }
	add_bullet_with_tooltip_effect = yes
}
hanza_satellite_trade_trade_scripted_effect = {
	ROOT = {
		add_opinion_modifier = {
			target = FROM
			modifier = hanza_satellite_trade
		}
	}

	add_timed_idea = {
		idea = hanza_satellite_trade_trade_modifier
		days = 45
	}

	set_temp_variable = { bullet_to_gain = -50 }
	add_bullet_with_tooltip_effect = yes
}